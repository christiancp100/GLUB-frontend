import axios from 'axios';
import {baseURL} from '../constants/URLs';


class ApiCaller {
    get = (url : string, parameters : Array<{key : string, value : string | number}> ) => {

    }

    post = (url : string, body : Array<{key : string, value : string | number}> ) => {

    }
}

export default new ApiCaller();